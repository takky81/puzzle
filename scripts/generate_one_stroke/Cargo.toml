[package]
name = "generate_one_stroke"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "generate_one_stroke"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = "1"

[profile.release]
opt-level = 3
